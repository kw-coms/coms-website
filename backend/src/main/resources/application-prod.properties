# PostgreSQL
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:comsdb}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# JPA -- validate only; Flyway manages schema
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:validate}

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
# Explicit env mapping for FlywayRepairConfig's fail-fast gate (see .env.example).
coms.flyway.auto-repair=${COMS_FLYWAY_AUTO_REPAIR:false}
