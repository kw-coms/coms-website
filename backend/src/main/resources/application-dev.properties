# Dev profile — activate with: --spring.profiles.active=dev
# or set SPRING_PROFILES_ACTIVE=dev

spring.h2.console.enabled=true
cookie.secure=false
jwt.secret=coms-dev-only-secret-key-do-not-use-in-production!!
mail.log-verification-codes=true

# JPA manages schema in dev (Flyway is for prod only)
spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=update

cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173
